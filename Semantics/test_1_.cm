int gcd (int u, int v)
{
	if (v == 0) return u;
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */

	return;
}

int main(void)
{
	int x;
	int y[5];
	int z;

	x = input();

	z = (
		x+
		y
	)*
	(
		x-t
	);

	output(gcd(x, y[0]));

	if (x+z) {}
	else {}

	while(x!=(y[7]+z)*(y[0]+y[1])) {}

	return 0;
}